# CLAUDE.md

This project uses `AGENT.md` as the single source of truth for implementation rules, project constraints, acceptance requirements, testing standards, and delivery expectations.

For any work on IronPage Vault, read `AGENT.md` first and follow it as the authoritative project guide.

Do not duplicate the full rule set here. Keep detailed requirements in `AGENT.md` only, so the project has one maintainable rule source.

If another project document appears to differ from `AGENT.md`, use `AGENT.md` as the reference and update the other document to stay consistent.
